        bool  hasProbabilities()
        bool  hasTracks()
      size_t  taggingVariables().size()
        bool  primaryVertex()->isValid()
        bool  primaryVertex()->isFake()
      size_t  primaryVertex()->tracksSize()
      double  primaryVertex()->chi2()
      double  primaryVertex()->ndof()
      double  primaryVertex()->normalizedChi2()
      double  primaryVertex()->x()
      double  primaryVertex()->y()
      double  primaryVertex()->z()
      double  primaryVertex()->xError()
      double  primaryVertex()->yError()
      double  primaryVertex()->zError()
      double  primaryVertex()->covariance(int i, int j)
        bool  primaryVertex()->hasRefittedTracks()
unsigned int  primaryVertex()->nTracks(float minWeight=5.0e-1)
        bool  ghostTrack()->outerOk()
        bool  ghostTrack()->innerOk()
unsigned int  ghostTrack()->outerDetId()
unsigned int  ghostTrack()->innerDetId()
      size_t  ghostTrack()->recHitsSize()
      double  ghostTrack()->outerPx()
      double  ghostTrack()->outerPy()
      double  ghostTrack()->outerPz()
      double  ghostTrack()->outerX()
      double  ghostTrack()->outerY()
      double  ghostTrack()->outerZ()
      double  ghostTrack()->outerP()
      double  ghostTrack()->outerPt()
      double  ghostTrack()->outerPhi()
      double  ghostTrack()->outerEta()
      double  ghostTrack()->outerTheta()
      double  ghostTrack()->outerRadius()
unsigned short  ghostTrack()->found()
unsigned short  ghostTrack()->lost()
      double  ghostTrack()->residualX(int position)
      double  ghostTrack()->residualY(int position)
      double  ghostTrack()->chi2()
      double  ghostTrack()->ndof()
      double  ghostTrack()->normalizedChi2()
         int  ghostTrack()->charge()
      double  ghostTrack()->qoverp()
      double  ghostTrack()->theta()
      double  ghostTrack()->lambda()
      double  ghostTrack()->dxy()
      double  ghostTrack()->d0()
      double  ghostTrack()->dsz()
      double  ghostTrack()->dz()
      double  ghostTrack()->p()
      double  ghostTrack()->pt()
      double  ghostTrack()->px()
      double  ghostTrack()->py()
      double  ghostTrack()->pz()
      double  ghostTrack()->phi()
      double  ghostTrack()->eta()
      double  ghostTrack()->vx()
      double  ghostTrack()->vy()
      double  ghostTrack()->vz()
      double  ghostTrack()->parameter(int i)
      double  ghostTrack()->covariance(int i, int j)
      double  ghostTrack()->error(int i)
      double  ghostTrack()->qoverpError()
      double  ghostTrack()->ptError()
      double  ghostTrack()->thetaError()
      double  ghostTrack()->lambdaError()
      double  ghostTrack()->etaError()
      double  ghostTrack()->phiError()
      double  ghostTrack()->dxyError()
      double  ghostTrack()->d0Error()
      double  ghostTrack()->dszError()
      double  ghostTrack()->dzError()
unsigned int  ghostTrack()->covIndex(unsigned int i, unsigned int j)
unsigned short  ghostTrack()->numberOfValidHits()
unsigned short  ghostTrack()->numberOfLostHits()
      double  ghostTrack()->validFraction()
         int  ghostTrack()->qualityMask()
        bool  ghostTrack()->isLooper()
