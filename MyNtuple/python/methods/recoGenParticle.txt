         int  collisionId()
      size_t  numberOfDaughters()
      size_t  numberOfMothers()
      size_t  numberOfSourceCandidatePtrs()
         int  charge()
         int  threeCharge()
      double  p()
      double  energy()
      double  et()
      double  mass()
      double  massSqr()
      double  mt()
      double  mtSqr()
      double  px()
      double  py()
      double  pz()
      double  pt()
      double  phi()
      double  theta()
      double  eta()
      double  rapidity()
      double  y()
      double  vx()
      double  vy()
      double  vz()
         int  pdgId()
         int  status()
        bool  longLived()
        bool  massConstraint()
      double  vertexChi2()
      double  vertexNdof()
      double  vertexNormalizedChi2()
      double  vertexCovariance(int i, int j)
        bool  hasMasterClone()
        bool  hasMasterClonePtr()
        bool  isElectron()
        bool  isMuon()
        bool  isStandAloneMuon()
        bool  isGlobalMuon()
        bool  isTrackerMuon()
        bool  isCaloMuon()
        bool  isPhoton()
        bool  isConvertedPhoton()
        bool  isJet()
unsigned int  longLivedTag
unsigned int  massConstraintTag
         int  daughterRef(size_t i)->collisionId()
      size_t  daughterRef(size_t i)->numberOfDaughters()
      size_t  daughterRef(size_t i)->numberOfMothers()
      size_t  daughterRef(size_t i)->numberOfSourceCandidatePtrs()
         int  daughterRef(size_t i)->charge()
         int  daughterRef(size_t i)->threeCharge()
      double  daughterRef(size_t i)->p()
      double  daughterRef(size_t i)->energy()
      double  daughterRef(size_t i)->et()
      double  daughterRef(size_t i)->mass()
      double  daughterRef(size_t i)->massSqr()
      double  daughterRef(size_t i)->mt()
      double  daughterRef(size_t i)->mtSqr()
      double  daughterRef(size_t i)->px()
      double  daughterRef(size_t i)->py()
      double  daughterRef(size_t i)->pz()
      double  daughterRef(size_t i)->pt()
      double  daughterRef(size_t i)->phi()
      double  daughterRef(size_t i)->theta()
      double  daughterRef(size_t i)->eta()
      double  daughterRef(size_t i)->rapidity()
      double  daughterRef(size_t i)->y()
      double  daughterRef(size_t i)->vx()
      double  daughterRef(size_t i)->vy()
      double  daughterRef(size_t i)->vz()
         int  daughterRef(size_t i)->pdgId()
         int  daughterRef(size_t i)->status()
        bool  daughterRef(size_t i)->longLived()
        bool  daughterRef(size_t i)->massConstraint()
      double  daughterRef(size_t i)->vertexChi2()
      double  daughterRef(size_t i)->vertexNdof()
      double  daughterRef(size_t i)->vertexNormalizedChi2()
      double  daughterRef(size_t i)->vertexCovariance(int i, int j)
        bool  daughterRef(size_t i)->hasMasterClone()
        bool  daughterRef(size_t i)->hasMasterClonePtr()
        bool  daughterRef(size_t i)->isElectron()
        bool  daughterRef(size_t i)->isMuon()
        bool  daughterRef(size_t i)->isStandAloneMuon()
        bool  daughterRef(size_t i)->isGlobalMuon()
        bool  daughterRef(size_t i)->isTrackerMuon()
        bool  daughterRef(size_t i)->isCaloMuon()
        bool  daughterRef(size_t i)->isPhoton()
        bool  daughterRef(size_t i)->isConvertedPhoton()
        bool  daughterRef(size_t i)->isJet()
unsigned int  daughterRef(size_t i)->longLivedTag
unsigned int  daughterRef(size_t i)->massConstraintTag
         int  motherRef(size_t i=0)->collisionId()
      size_t  motherRef(size_t i=0)->numberOfDaughters()
      size_t  motherRef(size_t i=0)->numberOfMothers()
      size_t  motherRef(size_t i=0)->numberOfSourceCandidatePtrs()
         int  motherRef(size_t i=0)->charge()
         int  motherRef(size_t i=0)->threeCharge()
      double  motherRef(size_t i=0)->p()
      double  motherRef(size_t i=0)->energy()
      double  motherRef(size_t i=0)->et()
      double  motherRef(size_t i=0)->mass()
      double  motherRef(size_t i=0)->massSqr()
      double  motherRef(size_t i=0)->mt()
      double  motherRef(size_t i=0)->mtSqr()
      double  motherRef(size_t i=0)->px()
      double  motherRef(size_t i=0)->py()
      double  motherRef(size_t i=0)->pz()
      double  motherRef(size_t i=0)->pt()
      double  motherRef(size_t i=0)->phi()
      double  motherRef(size_t i=0)->theta()
      double  motherRef(size_t i=0)->eta()
      double  motherRef(size_t i=0)->rapidity()
      double  motherRef(size_t i=0)->y()
      double  motherRef(size_t i=0)->vx()
      double  motherRef(size_t i=0)->vy()
      double  motherRef(size_t i=0)->vz()
         int  motherRef(size_t i=0)->pdgId()
         int  motherRef(size_t i=0)->status()
        bool  motherRef(size_t i=0)->longLived()
        bool  motherRef(size_t i=0)->massConstraint()
      double  motherRef(size_t i=0)->vertexChi2()
      double  motherRef(size_t i=0)->vertexNdof()
      double  motherRef(size_t i=0)->vertexNormalizedChi2()
      double  motherRef(size_t i=0)->vertexCovariance(int i, int j)
        bool  motherRef(size_t i=0)->hasMasterClone()
        bool  motherRef(size_t i=0)->hasMasterClonePtr()
        bool  motherRef(size_t i=0)->isElectron()
        bool  motherRef(size_t i=0)->isMuon()
        bool  motherRef(size_t i=0)->isStandAloneMuon()
        bool  motherRef(size_t i=0)->isGlobalMuon()
        bool  motherRef(size_t i=0)->isTrackerMuon()
        bool  motherRef(size_t i=0)->isCaloMuon()
        bool  motherRef(size_t i=0)->isPhoton()
        bool  motherRef(size_t i=0)->isConvertedPhoton()
        bool  motherRef(size_t i=0)->isJet()
unsigned int  motherRef(size_t i=0)->longLivedTag
unsigned int  motherRef(size_t i=0)->massConstraintTag
