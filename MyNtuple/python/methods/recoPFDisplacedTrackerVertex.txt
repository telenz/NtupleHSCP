        bool  displacedVertexRef()->isFake()
        bool  displacedVertexRef()->isLooper()
        bool  displacedVertexRef()->isNucl()
        bool  displacedVertexRef()->isNucl_Loose()
        bool  displacedVertexRef()->isNucl_Kink()
        bool  displacedVertexRef()->isConv()
        bool  displacedVertexRef()->isConv_Loose()
        bool  displacedVertexRef()->isConvertedBremm()
        bool  displacedVertexRef()->isK0()
        bool  displacedVertexRef()->isLambda()
        bool  displacedVertexRef()->isLambdaBar()
        bool  displacedVertexRef()->isKplus()
        bool  displacedVertexRef()->isKminus()
        bool  displacedVertexRef()->isKplus_Loose()
        bool  displacedVertexRef()->isKminus_Loose()
        bool  displacedVertexRef()->isBSM()
        bool  displacedVertexRef()->isValid()
      size_t  displacedVertexRef()->tracksSize()
      double  displacedVertexRef()->chi2()
      double  displacedVertexRef()->ndof()
      double  displacedVertexRef()->normalizedChi2()
      double  displacedVertexRef()->x()
      double  displacedVertexRef()->y()
      double  displacedVertexRef()->z()
      double  displacedVertexRef()->xError()
      double  displacedVertexRef()->yError()
      double  displacedVertexRef()->zError()
      double  displacedVertexRef()->covariance(int i, int j)
        bool  displacedVertexRef()->hasRefittedTracks()
