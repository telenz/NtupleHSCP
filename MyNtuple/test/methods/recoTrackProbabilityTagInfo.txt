         int  factorial(int n)
       float  probability(size_t n, int ip)
       float  jetProbability(int ip, float minTrackProb)
       float  discriminator(int ipType, float minProb)
         int  selectedTracks(int ipType)
         int  trackIndex(size_t n, int ip)
        bool  hasTracks()
        bool  track(size_t n, int ipType).outerOk()
        bool  track(size_t n, int ipType).innerOk()
unsigned int  track(size_t n, int ipType).outerDetId()
unsigned int  track(size_t n, int ipType).innerDetId()
      size_t  track(size_t n, int ipType).recHitsSize()
      double  track(size_t n, int ipType).outerPx()
      double  track(size_t n, int ipType).outerPy()
      double  track(size_t n, int ipType).outerPz()
      double  track(size_t n, int ipType).outerX()
      double  track(size_t n, int ipType).outerY()
      double  track(size_t n, int ipType).outerZ()
      double  track(size_t n, int ipType).outerP()
      double  track(size_t n, int ipType).outerPt()
      double  track(size_t n, int ipType).outerPhi()
      double  track(size_t n, int ipType).outerEta()
      double  track(size_t n, int ipType).outerTheta()
      double  track(size_t n, int ipType).outerRadius()
unsigned short  track(size_t n, int ipType).found()
unsigned short  track(size_t n, int ipType).lost()
      double  track(size_t n, int ipType).residualX(int position)
      double  track(size_t n, int ipType).residualY(int position)
      double  track(size_t n, int ipType).chi2()
      double  track(size_t n, int ipType).ndof()
      double  track(size_t n, int ipType).normalizedChi2()
         int  track(size_t n, int ipType).charge()
      double  track(size_t n, int ipType).qoverp()
      double  track(size_t n, int ipType).theta()
      double  track(size_t n, int ipType).lambda()
      double  track(size_t n, int ipType).dxy()
      double  track(size_t n, int ipType).d0()
      double  track(size_t n, int ipType).dsz()
      double  track(size_t n, int ipType).dz()
      double  track(size_t n, int ipType).p()
      double  track(size_t n, int ipType).pt()
      double  track(size_t n, int ipType).px()
      double  track(size_t n, int ipType).py()
      double  track(size_t n, int ipType).pz()
      double  track(size_t n, int ipType).phi()
      double  track(size_t n, int ipType).eta()
      double  track(size_t n, int ipType).vx()
      double  track(size_t n, int ipType).vy()
      double  track(size_t n, int ipType).vz()
      double  track(size_t n, int ipType).parameter(int i)
      double  track(size_t n, int ipType).covariance(int i, int j)
      double  track(size_t n, int ipType).error(int i)
      double  track(size_t n, int ipType).qoverpError()
      double  track(size_t n, int ipType).ptError()
      double  track(size_t n, int ipType).thetaError()
      double  track(size_t n, int ipType).lambdaError()
      double  track(size_t n, int ipType).etaError()
      double  track(size_t n, int ipType).phiError()
      double  track(size_t n, int ipType).dxyError()
      double  track(size_t n, int ipType).d0Error()
      double  track(size_t n, int ipType).dszError()
      double  track(size_t n, int ipType).dzError()
unsigned int  track(size_t n, int ipType).covIndex(unsigned int i, unsigned int j)
unsigned short  track(size_t n, int ipType).numberOfValidHits()
unsigned short  track(size_t n, int ipType).numberOfLostHits()
      double  track(size_t n, int ipType).validFraction()
         int  track(size_t n, int ipType).qualityMask()
        bool  track(size_t n, int ipType).isLooper()
      size_t  taggingVariables().size()
